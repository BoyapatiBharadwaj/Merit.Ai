[pytest]
pythonpath = .
testpaths = tests
markers =
    postgres: needs a real PostgreSQL server (see tests/test_migrations_postgres.py).
        Skips automatically when neither TEST_POSTGRES_URL nor the optional
        `pgserver` package is available, so a plain `pytest` still passes on a
        machine with no database installed.
